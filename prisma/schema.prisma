generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Register {
  id        Int      @id @default(autoincrement())
  createdAt DateTime @default(now())

  @@map("registers")
}
